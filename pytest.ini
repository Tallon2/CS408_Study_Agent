[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (require external services like ChromaDB)
    e2e: marks tests as end-to-end tests (require running server)
    slow: marks tests as slow-running
addopts = -ra --tb=short
